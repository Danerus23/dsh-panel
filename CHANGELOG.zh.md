# 版本历史 — DeepSeek Harness 面板

这是中文版本历史。它从 1.21.0 版开始维护；更早版本的历史只存在于 `CHANGELOG.md`（俄文）中，
该文件仍是内容的源头。英文文件 `CHANGELOG.en.md` 同理。

## 1.21.0 — 2026-09-21

### 新增

- **更新说明使用界面语言显示。** 面板从 GitHub 下载发布说明并原样显示——始终是俄文，因为说明
  是从俄文的 `CHANGELOG.md` 中截取的。现在发布正文带有三段机器标记（`en`、`ru`、`zh`），面板
  按界面语言选择对应的段落：先找所需语言，没有该段就用英文，而 1.21.0 之前的发布完全没有标记时
  就原样显示整段正文。在发布页面上英文在前，俄文与中文作为独立段落随后出现——正文中完全没有
  HTML，因此 1.21.0 之前的面板看到的是可读文字，而不是标签。
- **`CHANGELOG.en.md` 和 `CHANGELOG.zh.md`** — 英文与中文版本历史，章节结构与 `CHANGELOG.md`
  相同（`## <版本>`），从 1.21.0 开始维护。发布正文由 `tools\release-notes.ps1` 从三个文件装配，
  三个文件都随面板一起分发。
- **`tools\check-notes.ps1`** — 检查装配好的发布正文：三个段落齐全、版本与 `DshTray.csproj` 中的
  `<Version>` 一致、英文段落非空、标记顺序正确，且重复运行得到完全相同的文件。
- **winget 软件包**（`winget\Danerus23.DSHPanel*.yaml` 与 `tools\winget-manifest.ps1`）：软件包被
  winget 仓库接受后，即可用 `winget install Danerus23.DSHPanel` 安装面板。清单通过
  `winget validate`，脚本从**发布包**取版本与 SHA-256，除非明确允许，否则拒绝使用本地文件的校验和。
- **README 的窗口拼图**（`docs\demo.png`）——由四个真实画面组成（面板、设置、备份、托盘菜单）。
- **「关于面板」中说明了捐助的用途。** 支持链接下方新增三种语言的简短说明：面板免费、没有付费版本，
  捐助用于开发时间 —— 代码、构建，以及在干净 Windows 上的验证。

### 变更

- **静默安装不会再卡在看不见的窗口上。** 有两个障碍：Inno 的「This will install… Do you want to
  continue?」提示（`/SILENT` 和 `/VERYSILENT` 都不会关闭它，现在由 `DisableStartupPrompt=yes`
  移除，winget 清单还会传入 `/SP-`），以及 `[Code]` 中关于 .NET 或 Node 的消息框
  （`/SUPPRESSMSGBOXES` 同样不会抑制）——在无人值守安装中，这些消息现在写入安装日志，而不是弹出窗口。
- **更新窗口会说明完整历史在哪里。** 说明字段下方新增一行提示：说明使用界面语言，完整历史见
  面板旁边的 `CHANGELOG.md`。
- **`--update-check` 只打印俄文段落，不含机器标记**：这份控制台报告由所有者阅读，标记只会造成干扰。
- **窗口截图已按三种语言重新拍摄** —— README 中此前仍是 1.20.0 的截图。

### 修复

- **已安装程序列表中的面板版本不再滞后。** 面板会自行更新，而该列表条目由安装程序在安装时写入
  一次，因此其中长期保留着当时的版本。现在面板在启动时会核对并修正自己条目中的 `DisplayVersion`
  （`PanelRegistration.RefreshVersion`），而且只改这一个值：不会创建条目，不会触碰他人的条目，
  使用替换目录的检查运行则完全不触碰。这对 winget 尤其重要：它会把自己的版本与记录值比较，没有
  这项核对就可能提示「升级」到比正在运行的面板更旧的版本。
